# 快捷指令 Office 加载项 · 下一步计划

> 本文档与项目一同打包，记录当前交付状态与后续可推进事项。按优先级排列，前两项是“马上能做/该做”的。

---

## 一、当前已交付状态（基线）

- **命令面板 `COMMANDS` 共 84 项**：通用 10、仅 Word 22、仅 Excel 30（含共享「护眼模式」1；「高级替换」在 Word 与 Excel 各一项）。
- **简繁转换已升级为全量词典**：`src/lib/opencc-bundle.js`（opencc-js UMD 打包，离线内置，约 1.18MB），正确处理多义字，无需联网。
- **免服务器部署闭环**：`manifest.xml`（GitHub Pages）+ `setup.js`（一键填用户名）+ `manifest-local.xml`（本地兜底）。
- **静态校验全绿（现可由 `node publish.js --check` 一键复跑）**：`node --check` 四 JS 通过；`shortcuts.json` 41 动作/41 快捷键解析正常；两 manifest XML 格式良好且 `<FunctionName>` 集合完全一致；`KB`/`RIBBON` 与快捷键、功能区按钮 0 不一致。
- **未做**：实际点击效果需在真实 Office 中旁加载验证（本环境无法运行 Office）。

---

## 二、优先级 1 —— 部署上线（你现在就能做）

1. 跑 `node setup.js`，输入你的 GitHub 用户名。
2. `git init / add / commit / push` 到 `https://github.com/<你>/office-quick-commands`。
3. 仓库 `Settings → Pages`，Source 选 `main` 分支 `/ (root)`，约 1 分钟后生效。
4. Office 中「插入 → 我的加载项 → 上传我的加载项」，选 `manifest.xml`，**仅此一次**，之后每次打开都有「快捷指令」选项卡。
5. 没 GitHub？用方案二：`npx office-addin-dev-certs install` → `node server.js` → 上传 `manifest-local.xml`，但每次用之前要保持 `server.js` 运行。

---

## 三、优先级 2 —— 真实 Office 验收（必做）

以下命令依赖具体 API 行为，建议在 Word/Excel 里逐个点一遍，确认表现符合预期：

- **Excel**：转置、公式转值、按颜色求和、文本分列、定位空值、数据有效性下拉、查找重复值、加/去边框、隐藏/取消隐藏行列。
- **Word**：表格↔文本、删除空白页、插入页码（PAGE 域）、插入页眉/页脚、多级列表、简繁转换（重点试多义字：干杯→乾杯、里长→里長）。
- **通用**：导出 PDF（桌面版应成功，网页版可能受限）、护眼模式切换与刷新后记忆。

> 验收无误后即可对外日常使用；发现异常请把现象记下来，回到本项目修。

---

## 四、优先级 3 —— 图标与品牌

- `assets/` 下 `icon-16/32/80.png` 是占位图。正式分发前替换为自己的品牌图标（需 16/32/80 像素 PNG，透明背景最佳）。
- 替换后两个 manifest 的 `IconUrl` / `bt:Images` 无需改路径（已指向 `assets/`）。

---

## 五、优先级 4 —— 功能扩展候选（按价值排序）

1. **PowerPoint 支持**：新增 Host，复用通用命令（加粗/对齐/导出 PDF）+ PPT 专属（统一字号、批量替换文本、版式套用）。清单 `<Hosts>` 加 `<Host xsi:type="Presentation"/>`。
2. **高级查找替换（单文档，已完成）**：`doAdvancedReplace` 支持区分大小写 / 全字匹配，并记忆上次输入（localStorage）。**跨文档/工作簿批量**仍为开放项（Office.js 跨文档能力有限）。
3. **翻译**：接翻译 API（如 Azure/腾讯云），需配置密钥；建议做成可开关的“高级”分组，避免无密钥时报错。
4. **一键打包发布脚本（已完成）**：`publish.js` 已落地——`--check` 仅校验、`--dry` 打印 git 命令、`publish.js` 校验通过即 `git add/commit/push`；校验不通过绝不推送。
5. **设置 / 偏好面板**：把默认数字格式、分隔符、是否记忆护眼模式等做成可配置项（localStorage）。

---

## 六、优先级 5 —— 工程化加固

- **双语文档（已完成）**：README 已增补英文 summary（面向上架 AppSource）。
- **错误日志**：命令失败时统一收集到面板「状态」区，必要时导出日志便于排查。
- **单元测试**：用 `office-addin-mock` 或 JSDOM 对纯逻辑（如分列、大小写、简繁）写最小用例，CI 里跑 `node --check` + 测试。

---

## 七、优先级 6 —— 上架 Microsoft AppSource（可选，长期）

若打算对外分发而非仅自用旁加载：
- 需要隐私政策 URL、使用条款、支持邮箱、配套云服务等合规材料。
- 清单需改为集中式/托管式配置，走 Microsoft 审核。
- 当前架构（纯静态 + 无后端）基本满足，但需补齐上述合规件。

---

## 八、一句话路线图

**部署（含一键发布）→ 真机验收 → 换图标 → （按需）加 PPT/翻译 → （长期）上架。** 一键发布脚本已完成，部署链路已闭环。
